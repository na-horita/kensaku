const Form = () => {
    return(
        <form>
            <input type="text" name="keyword" placeholder="e.g. cat" />
            <button type="submit">Search</button>
        </form>
    );
}
 
export default Form;